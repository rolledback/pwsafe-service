{
  "name": "pwsafe-service-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preinstall": "git config core.hooksPath .githooks",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write . --log-level warn"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "prettier": "^3.8.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
